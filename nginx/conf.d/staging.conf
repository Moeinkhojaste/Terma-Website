# ==============================================================================
# Terma Staging Virtual Host Configuration
# Includes HTTP Basic Auth Protection & Strict NoIndex Headers
# ==============================================================================

# HTTP -> HTTPS Redirect + ACME Challenge
server {
    listen 80;
    listen [::]:80;
    server_name staging.termabrand.ir;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Staging HTTPS Server
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name staging.termabrand.ir;

    # SSL Certificates
    ssl_certificate /etc/nginx/ssl/live/staging.termabrand.ir/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/live/staging.termabrand.ir/privkey.pem;

    # Modern TLS Security Parameters
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:StageSSL:5m;
    ssl_session_tickets off;

    # ==========================================================================
    # Staging Access Protection: HTTP Basic Auth & No-Index
    # ==========================================================================
    auth_basic "Restricted Terma Staging Environment";
    auth_basic_user_file /etc/nginx/auth/.htpasswd;

    # Prevent Search Engine Indexing
    add_header X-Robots-Tag "noindex, nofollow, noarchive, nosnippet" always;
    add_header X-Environment "staging" always;

    # Security Headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Staging Backend API Routing
    location /api/ {
        set $staging_backend "staging-backend:8080";
        proxy_pass http://$staging_backend/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Staging Health Checks (Accessible for monitoring without basic auth if needed)
    location /health/ {
        auth_basic off;
        set $staging_backend "staging-backend:8080";
        proxy_pass http://$staging_backend/health/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Swagger Documentation (Enabled in Staging behind Basic Auth)
    location /swagger {
        set $staging_backend "staging-backend:8080";
        proxy_pass http://$staging_backend/swagger;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Next.js Static Asset Caching
    location /_next/static/ {
        set $staging_frontend "staging-frontend:3000";
        proxy_pass http://$staging_frontend/_next/static/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        expires 7d;
        access_log off;
    }

    # Staging Storefront Catch-all
    location / {
        set $staging_frontend "staging-frontend:3000";
        proxy_pass http://$staging_frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
