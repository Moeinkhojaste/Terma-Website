# جامع‌ترین گزارش ممیزی امنیتی و سئوی فنی ترما (Terma Security & Technical SEO Hardening Audit)

## ۱. خلاصه اجرایی و متدولوژی ارزیابی

این گزارش مستندسازی جامع، فنی و ساختاریافته از ممیزی امنیتی منطبق بر استانداردهای **OWASP Top 10:2025** و **OWASP ASVS 5.0.0 (سطح ۲)**، به همراه پیاده‌سازی زیرساخت سئوی فنی (Technical SEO) برای پلتفرم فروشگاهی **ترما (Terma)** است.

کلیه اقدامات اصلاحی با رعایت اصل جداسازی کامل معماری تمیز (Clean Architecture) در بک‌اند (`backend/`) و معماری مبتنی بر ویژگی (Feature-based Next.js App Router) در فرانت‌اند (`frontend/`) بدون ایجاد وابستگی متقابل پیاده‌سازی و اعتبارسنجی شده‌اند.

---

## ۲. ماتریس انطباق با OWASP Top 10:2025

| رده ریسک OWASP 2025 | آسیب‌پذیری‌های اولیه شناسایی‌شده | اقدامات اصلاحی و مقاوم‌سازی اعمال‌شده | وضعیت نهایی |
| :--- | :--- | :--- | :--- |
| **A01: Broken Access Control** | • امکان مشاهده محصولات و دسته‌های غیرفعال توسط عموم<br>• بازگرداندن مقادیر داخلی مانند حد آستانه انبار و موجودی رزرو به کاربر عادی<br>• امکان دسترسی به داده‌های سفارش از طریق توکن عمومی bearer | • تفکیک کامل DTOهای عمومی (`PublicProductDto`, `PublicCategoryDto`) از DTOهای ادمین (`ProductDto`, `CategoryDto`)<br>• حذف فیلد `TrackingToken` و اندپوینت پیگیری عمومی؛ احراز هویت سفارشات تنها از طریق پیامک تأیید تلفن همراه خریدار<br>• کنترل دسترسی رول‌محور برای پنل ادمین (`[Authorize(Roles = "Administrator")]`) | **ایمن و برطرف‌شده** |
| **A02: Cryptographic Failures** | • رمز عبور پیش‌فرض هاردکد شده ادمین در کد و راه‌اندازی تکرارپذیر ناامن<br>• فقدان انقضای امنیتی کلیدهای Data Protection در محیط‌های واقعی | • حذف کامل رمز پیش‌فرض؛ ایجاد پروسه امن `--seed-admin` با نیاز به پارامترهای محیطی و عدم بازنویسی ادمین‌های موجود<br>• الزام طول رمز عبور ۱۲ تا ۱۲۸ کاراکتر با پیچیدگی کامل<br>• پیکربندی اختصاصی ASP.NET Core Data Protection و قفل‌کردن تنظیمات بدون کلیدهای توسعه | **ایمن و برطرف‌شده** |
| **A03: Injection & Input Integrity** | • ورود داده‌های نامعتبر در فیلدهای طول، عرض، کدپستی و تلفن<br>• عدم اعتبارسنجی یکپارچه شناسه اسلاگ‌ها و فرمت‌های ورودی | • پیاده‌سازی FluentValidation در سطح لایه Application برای تمام دستورات و پرس‌وجوها<br>• نرمال‌سازی شماره تلفن و کد پستی ۱۰ رقمی ایران در لایه Domain<br>• اعتبارسنجی امن اسلاگ‌های فارسی (`PersianSlugHelper`) | **ایمن و برطرف‌شده** |
| **A04: Insecure Design** | • خطای Race Condition در فرآیند ثبت سفارش و اعمال کدهای تخفیف<br>• امکان ثبت همزمان و اتمام منفی موجودی انبار هنگام بار ترافیکی | • پیاده‌سازی موتور سفارش اتمیک در سطح انزوا (Serializable Transaction) با قفل همزمانی روی ویرایش ردیف‌های تنوع و کد تخفیف<br>• اعتبارسنجی اتمیک سهمیه مصرف کد تخفیف و بازگرداندن خطای ۴۰۹ در صورت بروز تعارض<br>• الزام هدر `Idempotency-Key` با اعتبارسنجی اثرانگشت SHA-256 درخواست | **ایمن و برطرف‌شده** |
| **A05: Security Misconfiguration** | • فعال بودن CORS باز localhost و دیتابیس موقت در محیط پروداکشن<br>• ارسال سرور هدرهای اضافه و فقدان هدرهای سخت‌گیرانه مرورگر | • فعال‌سازی مکانیزم Fail-Fast در محیط پروداکشن برای قطع آنی سرور در صورت فقدان تنظیمات محیطی امن<br>• تزریق خودکار هدرهای امنیتی `CSP`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Permissions-Policy`, `HSTS`<br>• اعمال `no-store` برای کلیه اندپوینت‌های حساس احراز هویت | **ایمن و برطرف‌شده** |
| **A06: Vulnerable & Outdated Components** | • ایمپورت فونت‌های تکراری خارجی بدون استفاده از قابلیت‌های فریم‌ورک | • استفاده انحصاری از پکیج‌های استاندارد دات‌نت ۸ و Next.js 16<br>• حذف `@import` فونت خارجی و استفاده از `next/font/google` با کش بهینه محلی | **ایمن و برطرف‌شده** |
| **A07: Identification & Authentication Failures** | • امکان حملات Brute-force روی لاگین ادمین و کدهای OTP پیامکی<br>• عدم ابطال چالش‌های OTP پس از مصرف یا انقضا | • سیستم قفل‌شدن (Lockout) حساب ادمین پس از ۵ بار تلاش ناموفق به مدت ۱۵ دقیقه<br>• یک‌بار مصرف بودن واقعی چالش‌های ورود پیامکی، محدودیت ۵ بار تلاش برای هر چالش و انقضای ۳ دقیقه‌ای<br>• کوکی‌های امن `HttpOnly`, `Secure`, `SameSite=Lax` با نشست ۲ ساعته ادمین | **ایمن و برطرف‌شده** |
| **A08: Software & Data Integrity Failures** | • خطر آپلود فایل‌های مخرب، اسکریپت‌های استتارشده و تصاویر Polyglot | • پیاده‌سازی خط لوله بهداشتی `ImageSanitizer` در لایه زیرساخت با بررسی هدرهای باینری، محدودیت ۲۵ مگاپیکسل و بازتولید/رمزگذاری مجدد (Re-encoding) تصاویر و پاک‌سازی کامل متادیتای EXIF | **ایمن و برطرف‌شده** |
| **A09: Security Logging & Monitoring Failures** | • عدم ثبت لاگ‌های امنیتی تغییر وضعیت، ورود و دسترسی‌های ناموفق | • پیاده‌سازی جدول و موجودیت لاگ امنیتی فقط-افزودنی (`SecurityAuditEvent`) با ذخیره عامل، عملیات، آدرس مبدا، TraceId و پیامد بدون ذخیره اسرار | **ایمن و برطرف‌شده** |
| **A10: Server-Side Request Forgery & CSRF** | • تغییر وضعیت‌های CMS Preview با درخواست GET بدون بررسی Origin<br>• امکان جعل درخواست در اندپوینت‌های خروج و جهش داده | • تغییر تمامی اندپوینت‌های فعال‌سازی و خروج پیش‌نمایش CMS به درخواست‌های `POST` با اعتبارسنجی سفت‌وسخت هدر Origin<br>• اعمال فیلتر دوگانه توکن ضد جعل (`ValidateApiAntiforgeryToken`) برای هردو نشست ادمین و مشتری | **ایمن و برطرف‌شده** |

---

## ۳. جزییات کنترل‌های پیاده‌سازی‌شده OWASP ASVS 5.0.0 (سطح ۲)

### V1: معماری، طراحی و مدل‌سازی تهدید
- رعایت اصل کمترین سطح دسترسی برای حساب دیتابیس و جداسازی عملیات مایگریشن از طریق فرمان `--migrate`.
- ممانعت از اجرای عملیات سنگین یا پرریسک در خارج از محدوده تراکنش‌های پایگاه‌داده.

### V2: احراز هویت
- احراز هویت پیامکی مبتنی بر کدهای تصادفی ایمن، هش‌شده با کلید مجزا (`OtpSecretKey`) و پیگیری وضعیت چالش.
- جلوگیری از فاش‌شدن وجود یا عدم وجود شماره همراه در پاسخ‌های عمومی احراز هویت.

### V3: مدیریت نشست (Session Management)
- تفکیک کامل شناسه نشست ادمین (`Identity.Application`) و نشست خریدار (`CustomerSession`).
- ابطال کوکی‌ها در هنگام خروج با الزامی بودن اعتبارسنجی Antiforgery Token برای جلوگیری از حملات CSRF Logout.

### V4: کنترل دسترسی
- مدل‌سازی مستقیم مالکیت اشیاء (Object-Level Authorization)؛ خریداران صرفاً امکان مشاهده تاریخچه سفارشات متصل به شماره همراه تأییدشده خود را دارند.
- مسدودسازی دسترسی به روت‌های مدیریتی `/api/admin/*` برای کاربران عادی و مهمان.

### V5: اعتبارسنجی ورودی و پاک‌سازی
- تطابق سخت‌گیرانه با فرمت‌های مجاز پستی (۱۰ رقم)، موبایل استاندارد ایرانی (`09xxxxxxxxx`) و ابعاد میز (طول و عرض بر حسب سانتی‌متر).
- محدودیت متنی در فیلدهای توضیحات و نظرات مشتری برای جلوگیری از حملات سرریز داده و XSS.

### V8: حفاظت از داده‌ها و حریم خصوصی
- عدم انتشار فیلدهای رزرو، حاشیه سود یا اطلاعات هویتی سایر مشتریان در کاتالوگ عمومی.
- پاک‌سازی متادیتای موقعیت جغرافیایی و تجهیزات عکس‌برداری (EXIF/GPS) از تصاویر بارگذاری‌شده.

### V11: منطق تجاری (Business Logic)
- موتور سفارش‌گیری تک‌مرحله‌ای و محاسبه نقل‌قول‌ها (`Quote`) در سمت سرور؛ جلوگیری از امکان دستکاری قیمت توسط کلاینت.
- اعتبارسنجی همزمانی موجودی و سقف استفاده از کدهای تخفیف با بازگرداندن خطای واضح ۴۰۹ به کاربر در صورت تغییرات همزمان.

---

## ۴. کنترل‌های نرخ درخواست (Partitioned Rate Limiting)

سیستم محدودکننده نرخ درخواست در `Program.cs` بر اساس IP و دسته‌بندی عملیات پیکربندی شده است:

| سیاست محدودکننده | سقف مجاز | پنجره زمانی | رفتار در صورت نقض |
| :--- | :--- | :--- | :--- |
| **ورود مدیران (`admin-login`)** | ۵ درخواست | ۱۵ دقیقه | خطای ۴۲۹ با هدر `Retry-After` |
| **درخواست کد OTP (`otp-request`)** | ۵ درخواست | ۱۰ دقیقه | خطای ۴۲۹ با پیام مناسب |
| **بررسی کد OTP (`otp-verify`)** | ۱۰ درخواست | ۱۰ دقیقه | مسدودسازی تا پایان پنجره |
| **ثبت سفارش نهایی (`orders`)** | ۵ درخواست | ۱۰ دقیقه | ممانعت از حملات ایجاد سفارش اسپم |
| **محاسبه قیمت سبد (`quotes`)** | ۲۰ درخواست | ۱ دقیقه | کنترل بار سرور و محاسبات مالی |
| **ارسال پیام تماس با ما (`contact`)** | ۵ درخواست | ۱۰ دقیقه | ممانعت از ارسال پیام‌های هرزنامه |

---

## ۵. معماری سئوی فنی (Technical SEO Hardening)

### ۵.۱. ساختار اسلاگ‌های فارسی و ریدایرکت‌های ۳۰۸
- ایجاد فیلد یکتا و ایندکس‌شده `Slug` برای تمامی محصولات و دسته‌بندی‌ها با نگاشت حروف فارسی استاندارد (`PersianSlugHelper`).
- مسیرهای استاندارد و کانونیکال به شکل `/products/{persian-slug}` و `/categories/{persian-slug}`.
- تشخیص خودکار شناسه‌های قدیمی مبتنی بر GUID و اعمال **ریدایرکت دائمی HTTP 308** به نشانی اسلاگ معتبر.

### ۵.۲. داده‌های ساختاریافته Schema.org JSON-LD
1. **صفحه محصول (`Product` & `Offer`)**:
   - نام، کد انبار (`SKU`)، توضیحات کامل و گالری تصاویر.
   - پیشنهاد فعال با واحد استاندارد بین‌المللی **ریال (IRR)** (معادل ۱۰ برابر قیمت تومانی).
   - وضعیت موجودی `InStock` یا `OutOfStock` بر اساس تعداد واقعی انبار.
   - سلسله‌مراتب مسیر ناوبری (`BreadcrumbList`).
   - عدم استفاده از نمرات و نظرات ساختگی جهت رعایت دستورالعمل‌های Google Search Quality Rater.

2. **صفحه دسته‌بندی (`CollectionPage`)**:
   - اطلاعات موضوعی دسته و لینک کانونیکال به همراه `BreadcrumbList`.

3. **چیدمان اصلی (`RootLayout`)**:
   - ساختار داده‌ای `WebSite` دارای قابلیت جست‌وجو (`SearchAction`).
   - ساختار داده‌ای `Organization` شامل لوگوی رسمی، تلفن پشتیبانی و قلمرو جغرافیایی ایران (`IR`).

### ۵.۳. نقشه سایت پویا و فایل Robots
- پیاده‌سازی اندپوینت بک‌اند `/api/store/seo/sitemap` و کامپوننت Next.js `src/app/sitemap.ts` با بروزرسانی تاریخ واقعی تغییرات، اولویت و فرکانس صفحات.
- پیاده‌سازی `src/app/robots.ts` حساس به متغیر محیطی `SITE_INDEXING_ENABLED` با بلاک کردن مسیرهای سیستمی `/admin/*`, `/checkout/*`, `/account/*`.
- اعمال تگ‌های متای ربات `noindex, nofollow` در صفحات خصوصی و پنل مدیریت.

---

## ۶. خلاصه نتایج آزمون‌ها و اعتبارسنجی اجرایی

### نتایج تست بک‌اند (ASP.NET Core .NET 8)
- **Unit Tests (`Terma.UnitTests`)**: ۴۲ تست موفق از ۴۲ تست (۱۰۰٪ قبولی)
- **Integration Tests (`Terma.IntegrationTests`)**: ۴۲ تست موفق از ۴۲ تست (۱۰۰٪ قبولی)
- **مجموع تست‌های بک‌اند**: **۸۴ تست موفق بدون هیچ‌گونه خطا یا هشدار**
- آزمون‌های امنیتی و اعتبارسنجی: تست موفق اعتبارسنجی کلید تکرارناپذیری (Idempotency Key Conflict 409)، تست پیشگیری از IDOR، تست جستجوی اسلاگ‌های فارسی، و تست دفع بارگذاری فایل‌های مخدوش و مخرب.

### نتایج آزمون فرانت‌اند (Next.js 16 / TypeScript / Vitest)
- **تست‌های واحد و یکپارچگی Vitest**: ۲۸ تست موفق از ۲۸ تست (۱۰۰٪ قبولی)
- **بررسی صحت تایپ‌ها (`npm run typecheck`)**: بدون خطا (۰ Error)
- **بررسی استایل و خطایابی کد (`npm run lint`)**: بدون خطا و هشدار (۰ Problem)
- **بیلد تولیدی Next.js (`npm run build`)**: بیلد موفق ۳۲ مسیر ایستا و پویا با بهینه‌سازی کامل تصاویر و رعایت سیاست‌های امنیتی هدرها.

---
*گزارش ممیزی امنیتی و سئوی فنی ترما با موفقیت تدوین و در سیستم ثبت گردید.*
