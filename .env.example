# Server Host (Your VPS IP address or your domain once purchased)
SERVER_HOST=87.107.128.201

# Database Configuration (Must be strong: uppercase, lowercase, digits, symbols)
DB_PASSWORD=TermaSecretDb_2026!#Strong

# Application Secrets (Must be at least 32 characters)
OTP_HASH_KEY=TermaProduction_OtpSecretKey_9876543210_Secure!

# Security Settings (Set to true once you have a domain with HTTPS/SSL)
REQUIRE_SECURE_COOKIES=false
ENFORCE_HTTPS=false
ENABLE_SWAGGER=true

# Admin Account Seed Credentials
ADMIN_EMAIL=admin@terma.local
ADMIN_PASSWORD=AdminSecurePass123!
